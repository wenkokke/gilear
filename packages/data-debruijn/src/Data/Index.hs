module Data.Index (
  Ix (FZ, FS),
  fromIx,
  fromIxRaw,
  isPos,
  thin,
  thick,
  inject,
  raise,
) where

import Data.Index.Unsafe (
  Ix (FS, FZ),
  fromIx,
  fromIxRaw,
  inject,
  isPos,
  raise,
  thick,
  thin,
 )
